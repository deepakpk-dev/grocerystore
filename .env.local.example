# Leave blank while using the mock catalog and placeholder business details
NEXT_PUBLIC_HIDE_MOCK_RIBBON=

# Optional locally; production uses the deployed hostname automatically
NEXT_PUBLIC_SITE_URL=http://localhost:3000
